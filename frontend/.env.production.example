# 激光切割生产管理系统 - 前端生产环境配置示例
# 复制此文件为 .env.production.local 并根据需要修改配置

# Node.js环境
NODE_ENV=production

# ===== 后端API配置 =====
# 生产环境建议使用完整URL方式

# 方式1: 完整URL（推荐用于生产环境）
NEXT_PUBLIC_BACKEND_URL=https://api.yourcompany.com

# 方式2: 主机+端口（仅用于同服务器部署）  
# NEXT_PUBLIC_BACKEND_HOST=yourserver.com
# NEXT_PUBLIC_BACKEND_PORT=35001

# 前端端口（通常生产环境使用80或443）
NEXT_PUBLIC_FRONTEND_PORT=80

# API请求超时时间（毫秒，生产环境建议更长）
NEXT_PUBLIC_API_TIMEOUT=60000

# ===== 常见生产部署场景 =====

# 场景1: 前后端同服务器（不同端口）
# NEXT_PUBLIC_BACKEND_HOST=localhost
# NEXT_PUBLIC_BACKEND_PORT=35001
# NEXT_PUBLIC_FRONTEND_PORT=80

# 场景2: 前后端分离部署（不同域名）
# NEXT_PUBLIC_BACKEND_URL=https://api.yourcompany.com
# NEXT_PUBLIC_FRONTEND_PORT=443

# 场景3: 前后端同域名（不同路径）
# NEXT_PUBLIC_BACKEND_URL=https://yourcompany.com/api
# NEXT_PUBLIC_FRONTEND_PORT=443