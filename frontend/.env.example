# 激光切割生产管理系统 - 前端环境配置示例
# 复制此文件为 .env.local 并根据需要修改配置

# Node.js环境 (development | production | test)
NODE_ENV=development

# ===== 后端API配置 =====
# 支持两种配置方式，选择其中一种：

# 方式1: 完整URL（用于分离部署，前后端在不同服务器）
# NEXT_PUBLIC_BACKEND_URL=https://api.yourcompany.com

# 方式2: 主机+端口（用于本地开发或同网段部署）
NEXT_PUBLIC_BACKEND_HOST=localhost
NEXT_PUBLIC_BACKEND_PORT=35001

# 前端端口配置
NEXT_PUBLIC_FRONTEND_PORT=4000

# API请求超时时间（毫秒）
NEXT_PUBLIC_API_TIMEOUT=30000

# ===== 分离部署示例 =====
# 当前端和后端部署在不同服务器时：
# NEXT_PUBLIC_BACKEND_URL=https://api.yourcompany.com
# NEXT_PUBLIC_FRONTEND_PORT=80

# ===== 本地开发示例 =====
# NEXT_PUBLIC_BACKEND_HOST=localhost
# NEXT_PUBLIC_BACKEND_PORT=35001
# NEXT_PUBLIC_FRONTEND_PORT=4000

# ===== 局域网部署示例 =====
# NEXT_PUBLIC_BACKEND_HOST=192.168.1.100
# NEXT_PUBLIC_BACKEND_PORT=35001
# NEXT_PUBLIC_FRONTEND_PORT=4000